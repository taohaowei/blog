server:
  port: 8080

spring:
  application:
    name: blog

  thymeleaf:
    mode: HTML5
    encoding: utf-8
    servlet:
      content-type: text/html
    cache: false
logging:
  config: classpath:logback-boot.xml
